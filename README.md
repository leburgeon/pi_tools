# pi_tools
A collection of tools for the raspberry pi and a LCD 1602 screen, for limiting _big_ screen time.

# Requirements:
#### I2C and Smbus:
- Must have I2C enabled on the pi
- Must have Smbus (system management bus) installed `sudo apt-get install python3-smbus`
